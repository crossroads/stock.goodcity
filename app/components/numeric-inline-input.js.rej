diff a/app/components/numeric-inline-input.js b/app/components/numeric-inline-input.js	(rejected hunks)
@@ -101,2 +101,2 @@ export default Ember.TextField.extend({
-    const dummy = Math.random()
-    (newValue === "" || newValue === null) && (newValue = dummy);
+    const dummy =
+      Math.random()(newValue === "" || newValue === null) && (newValue = dummy);
