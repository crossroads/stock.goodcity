<li class="added-item-list">
  <div class="flex">
    <div class="thumb-container">
      <img src={{package.thumbImageUrl}} class="thumb" />
    </div>
    <div class="details">
      <div class="content">
        <span>
          {{fa-icon (if package.isPartOfSet 'tags' (storage-icon package.storageTypeName))}}
          {{package.inventoryNumber}}
        </span>
      </div>
      <div class="content">
        <span>{{pkg.addedQuantity}} x {{package.notes}}</span>
      </div>
      <div class="content">
        {{package.notes}}
      </div>
    </div>
    {{#unless disableRemove}}
      <div class="close-button">
        {{!-- <div {{action 'unpack' entity package pkg.addedQuantity onLocationSelect onUnpack}}> --}}
        <div {{action 'beginUnpack' entity package pkg.addedQuantity target=packageService}}>
          {{fa-icon 'times-circle' size="lg"}} <span>{{t 'box_pallet.remove'}}</span>
        </div>
      </div>
    {{/unless}}
  </div>
</li>
